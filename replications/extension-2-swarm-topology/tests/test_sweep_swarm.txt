cd ~/Desktop/santos-pacheco-lenaerts-2006-replication/replications/extension-2-swarm-topology
python3 -c "
from sweep_swarm import sweep_swarm
T_values = [0.5, 1.0, 1.5, 2.0]
S_values = [-1.0, -0.5, 0.0, 0.5]
results = sweep_swarm('scale_free', n=200, generations=100, T_values=T_values, S_values=S_values,
                      R=1, P=0, num_realizations=10, seed=1, tail=10)
print('S \\\\ T  ' + '  '.join(f'{t:5.1f}' for t in T_values))
for S in S_values:
    print(f'{S:5.1f}  ' + '  '.join(f'{results[(T, S)]:5.2f}' for T in T_values))
"

Expected output (about 6 seconds):
S \ T    0.5    1.0    1.5    2.0
 -1.0   0.33   0.14   0.13   0.12
 -0.5   0.91   0.27   0.14   0.12
  0.0   0.97   0.87   0.17   0.14
  0.5   0.96   0.92   0.48   0.26

Why: the same small 4x4 grid as ../../../tests/test_sweep.txt, run through swarm
instead of imitation. The PATTERN is the check: cooperation highest
bottom-left of the T axis (low T) and top of the S axis (high S), falling
as T rises or S drops. If the high-T, low-S corner had the MOST
cooperation, something would be backwards.

Two expected differences from imitation's table (not bugs):
  - Swarm never hits exactly 0.00 in the harsh corner (it floors around
    0.12). Continuous positions near the 0.5 threshold keep flickering
    across it, so a few agents are always "C" by chance.
  - Swarm is lower than imitation in the middle of the grid (e.g. T=1.5,
    S=0.0: 0.17 vs. imitation's 0.33) -- the personal-best anchoring to
    defection described in ../extension-1-swarm-learning/CONCLUSIONS.txt.
